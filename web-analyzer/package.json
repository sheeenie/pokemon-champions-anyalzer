{
  "name": "pokemon-champions-analyzer",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "NODE_OPTIONS=\"--dns-result-order=ipv4first --no-network-family-autoselection\" vite",
    "build": "NODE_OPTIONS=\"--dns-result-order=ipv4first --no-network-family-autoselection\" vite build",
    "preview": "NODE_OPTIONS=\"--dns-result-order=ipv4first --no-network-family-autoselection\" vite preview",
    "build-sprites": "NODE_OPTIONS=\"--dns-result-order=ipv4first --no-network-family-autoselection\" node scripts/build-sprite-db.js"
  },
  "devDependencies": {
    "vite": "^8.2.0"
  },
  "dependencies": {
    "@napi-rs/canvas": "^1.0.7",
    "tesseract.js": "^7.0.0"
  }
}
